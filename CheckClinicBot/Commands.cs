namespace CheckClinic.Bot
{
    internal enum Commands
    {
        Unknown,
        SelectItem,
        Refresh,
        Back,
        Subscribe,
        Unsubscribe,

        RefreshByCommand,
    }
}
